<div class="card" id="{{this._id}}">
  <div class="card-block">
    <h4 class="card-title">{{this.title}}</h4>
    <p class="card-text">{{this.content}}</p>
    <button class="deleteComment btn btn-link" data-comment-id="{{this._id}}">Delete</button>
  </div>
</div>
